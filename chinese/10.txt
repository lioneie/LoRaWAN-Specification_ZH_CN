
10 B类模式的上行链路帧

B类模式中的上行链路帧与A类上行链路相同，但帧头中的FCtrl字段中的RFU位除外。 在A类上行链路中，该位未使用（RFU）。 该位用于B类上行链路。

在上行链路中设置为1的B类比特向网络服务器发信号通知该设备切换到B类模式并且现在准备接收预定的下行链路ping。

用于下行链路的FPending比特的表示未改变，并且仍然表示一个或多个下行链路帧在服务器中为该设备排队，并且设备应该保持接收机开启，如A类规范中所述。